{
  "name": "fit-pdf",
  "version": "1.0.2",
  "description": "Open PDFs using Fit Height by default in Obsidian.",
  "main": "main.js",
  "scripts": {
    "build": "node --check main.js && node -e \"const fs=require('fs'); for (const file of ['manifest.json','versions.json']) JSON.parse(fs.readFileSync(file,'utf8'));\"",
    "install:vault": "scripts/install-to-vault.sh",
    "test": "npm run build && node --test tests/install-to-vault.test.js"
  },
  "keywords": [
    "obsidian",
    "obsidian-plugin",
    "pdf",
    "fit-height"
  ],
  "author": "Tomas Gutierrez",
  "license": "MIT",
  "devDependencies": {}
}
